# Gallery

This directory is reserved for public, reviewed examples.

For the initial Community Edition, use `examples/briefs/community-briefs.json` and `examples/records/community-records.json` as text-first cases. Add output images only after confirming that the image and its source assets are safe to redistribute.
